---
description: SweetPotato theme memory — sync via SweetPotatOs; fastfetch ASC; lid sleep
alwaysApply: true
---

# SweetPotato context

Read `NOTES.md`. For ISO/build/SourceForge details, also read `../SweetPotatOs/NOTES.md` when that sibling repo is present.

- This repo is the theme source of truth; SweetPotatOs copies from here via `sync-theme.sh`.
- Fastfetch: ASCII `SPLogo.asc` only as primary logo (not chafa/PNG).
- Lid suspend via logind drop-in; caffeine must not block lid close.
