[Service]
# Archcraft sets PATH=/usr/share/archcraft/scripts only — glycin then cannot find /usr/bin/bwrap
# and the GTK file chooser (Firefox Save As) aborts.
Environment=PATH=/usr/local/bin:/usr/bin:/bin:/usr/share/archcraft/scripts
Environment=GDK_BACKEND=wayland,x11
