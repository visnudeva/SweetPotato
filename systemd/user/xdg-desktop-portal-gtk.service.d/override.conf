[Service]
# Ensure a real PATH so glycin can find /usr/bin/bwrap — otherwise the GTK
# file chooser (Firefox Save As) aborts on broken session PATH overrides.
Environment=PATH=/usr/local/bin:/usr/bin:/bin
Environment=GDK_BACKEND=wayland,x11
